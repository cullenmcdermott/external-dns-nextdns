# Depot CI Migration
# Source: .github/workflows/release.yml
#
# Changes made:
# - Changed GitHub runs-on labels to their Depot equivalents

name: Release
on:
  push:
    tags:
      - 'v*'
permissions:
  contents: write
  packages: write
jobs:
  release:
    name: Build and Release
    runs-on: depot-ubuntu-latest
    env:
      FLOX_DISABLE_METRICS: "true"
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Restore Nix cache
        uses: actions/cache@v4
        id: nix-cache
        with:
          path: /nix
          key: nix-${{ hashFiles('.flox/env/manifest.toml') }}
          restore-keys: |
            nix-
      - name: Install Flox
        if: steps.nix-cache.outputs.cache-hit != 'true'
        uses: flox/install-flox-action@v2
      - name: Ensure nix-daemon is running
        run: sudo systemctl start nix-daemon || sudo nix-daemon &
      - name: Activate Flox environment
        run: flox activate -- true
      - name: Run Dagger Release Pipeline
        run: >-
          flox activate -- dagger call release
          --source=.
          --version=${{ github.ref_name }}
          --registry-address=ghcr.io
          --registry-username=${{ github.actor }}
          --registry-password=env:GITHUB_TOKEN
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Generate Changelog
        run: flox activate -- dagger call changelog --source=. export --path=CHANGELOG.md
        continue-on-error: true
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: CHANGELOG.md
          draft: false
          prerelease: ${{ contains(github.ref, '-rc') || contains(github.ref, '-beta') || contains(github.ref, '-alpha') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
